# 記憶力じゃんけんアプリ 仕様書

## 1. 概要

ユーザーが「グー」「チョキ」「パー」のいずれかを選択し、コンピューターとじゃんけんを行うWebアプリ。
n回コンピュータと勝負をした後は、自分のx回前の手に勝ち続けるスコアを競う。
勝敗結果、選択内容、対戦履歴、スコアを画面に表示する。

## 3. 対象ユーザー

- じゃんけんで簡単に遊びたい人
- 簡単な脳トレをしたい人

## 4. 画面構成

### 4.1 ヘッダー

- アプリ名を表示する
- 表示テキスト例: `じゃんけん記憶アプリ`

### 4.2 プレイエリア

- ユーザーが選択するボタンを3つ表示する
  - グー
  - チョキ
  - パー
- ボタンには絵文字を表示する
- ボタンを押すと1回勝負を実行する

### 4.3 結果表示エリア

以下を表示する。

- 自分の手
- コンピューターの手
- 勝敗結果
  - 勝ち
  - 負け
  - あいこ

### 4.4 スコア表示エリア

以下の累計を表示する。

- 対戦数

### 4.5 履歴エリア

直近の対戦履歴を表示する。

表示項目:

- 何戦目か
- 自分の手
- コンピューターの手
- 勝敗

履歴は新しいものが上に表示される。

### 4.6 操作ボタン

- リセットボタン
  - スコアと履歴を初期化する
- もう一度遊ぶボタン
  - 結果表示だけを初期状態に戻す
  - 省略可

## 5. 機能要件

### 5.1 手の選択

- ユーザーは「グー」「チョキ」「パー」から1つ選択できる
- 選択後、すぐに勝敗判定を行う

### 5.2 コンピューターの手

- コンピューターの手はランダムで決定する
- 候補は以下の3つ
  - グー
  - チョキ
  - パー

### 5.3 勝敗判定

判定ルール:

| 自分 | コンピューター | 結果 |
| --- | --- | --- |
| グー | チョキ | 勝ち |
| グー | パー | 負け |
| グー | グー | あいこ |
| チョキ | パー | 勝ち |
| チョキ | グー | 負け |
| チョキ | チョキ | あいこ |
| パー | グー | 勝ち |
| パー | チョキ | 負け |
| パー | パー | あいこ |

### 5.4 スコア更新

- 勝負するたびに対戦数を1増やす
- 結果に応じて勝ち数、負け数、あいこ数を更新する
- 勝率は以下で計算する

```text
勝率 = 勝ち数 / 対戦数 * 100
```

- 小数点は0桁または1桁まで表示する

### 5.5 履歴保存

- 勝負するたびに履歴を追加する
- 履歴は最低5件表示する
- 余裕があれば10件まで表示する

### 5.6 リセット

- リセットボタンを押すと以下を初期化する
  - 対戦数
  - 勝ち数
  - 負け数
  - あいこ数
  - 勝率
  - 履歴
  - 結果表示

### 5.7 保存機能

- `sessionStorage` を使う
  - 同じタブを開いている間だけスコアと履歴を保持する

保存対象:

- スコア
- 履歴

## 6. 非機能要件

### 6.1 対応環境

- Google Chrome 最新版
- PC表示を基本とする
- 余裕があればスマートフォン幅にも対応する

### 6.2 表示

- 文字が読みやすいこと
- ボタンが押しやすいこと
- 結果がひと目で分かること
- 勝ち、負け、あいこで表示色や演出を変えてもよい

### 6.3 アクセシビリティ

- ボタンには分かりやすいラベルをつける
- 画像を使う場合は `alt` を設定する
- 色だけで勝敗を判断させない

## 7. ディレクトリ構成案

```text
20260515_janken/
├── SPEC.md
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
└── img/
    ├── gu.png
    ├── choki.png
    └── pa.png
```

## 8. 実装方針

### 8.1 HTML

- ヘッダーを作る
- じゃんけんボタンを3つ作る
- 結果表示エリアを作る
- スコア表示エリアを作る
- 履歴表示エリアを作る
- リセットボタンを作る

### 8.2 CSS

- 画面中央にコンテンツを配置する
- じゃんけんボタンを横並びにする
- 結果表示を目立たせる
- 履歴は表またはリストで表示する

### 8.3 JavaScript

必要な処理:

- ユーザーの手を取得する
- コンピューターの手をランダムで決める
- 勝敗を判定する
- 結果を画面に表示する
- スコアを更新する
- 履歴を追加する
- リセットする
- 保存機能を使う場合はストレージに保存・復元する

## 9. 変数・データ設計案

```js
const hands = ["グー", "チョキ", "パー"];

const score = {
  total: 0,
  win: 0,
  lose: 0,
  draw: 0
};

const history = [
  {
    round: 1,
    player: "グー",
    computer: "チョキ",
    result: "勝ち"
  }
];
```

## 10. 画面文言案

- アプリ名: `じゃんけんアプリ`
- 説明文: `出す手を選んで、コンピューターと勝負しよう`
- 結果初期表示: `手を選んでください`
- 勝ち: `あなたの勝ち！`
- 負け: `あなたの負け...`
- あいこ: `あいこ！`
- リセット: `リセット`

## 11. 追加アイデア

余裕があれば実装する。

- 効果音を鳴らす
- 勝敗に応じて画像やアニメーションを変える
- 3回勝負モード
- 5回勝負モード
- CPUの表情を変える
- スコアをランキング風に表示する
- おみくじ画像など別課題の素材と組み合わせる

## 12. 完成条件

- 3つの手をクリックできる
- コンピューターの手がランダムに決まる
- 勝敗が正しく表示される
- スコアが更新される
- 履歴が追加される
- リセットできる
- Chromeで表示崩れなく確認できる

## 13. テスト項目

| No | 確認内容 | 期待結果 |
| --- | --- | --- |
| 1 | グーを押す | 勝敗結果が表示される |
| 2 | チョキを押す | 勝敗結果が表示される |
| 3 | パーを押す | 勝敗結果が表示される |
| 4 | 何度か勝負する | 対戦数が増える |
| 5 | 勝った場合 | 勝ち数が増える |
| 6 | 負けた場合 | 負け数が増える |
| 7 | あいこの場合 | あいこ数が増える |
| 8 | リセットを押す | スコアと履歴が初期化される |
| 9 | 再読み込みする | 保存機能を入れた場合、スコアと履歴が復元される |

